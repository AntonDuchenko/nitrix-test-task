{
  "name": "nitrix-test-task",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start:dev": "npm-run-all --parallel start:dev:server start:dev:frontend",
    "init": "npm-run-all --parallel init:server init:frontend",
    "start:dev:server": "npm run start:dev -w server",
    "start:dev:frontend": "npm run start:dev -w frontend",
    "init:server": "npm run init -w server",
    "init:frontend": "npm run init -w frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "server",
    "frontend"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}